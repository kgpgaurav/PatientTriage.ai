# Copy this file to .env and adjust if your API isn't on the default port/host.
# The app also lets you change this at runtime via the "reconnect" field in the nav bar,
# which takes precedence over this value once set.
VITE_API_BASE=http://localhost:8000
